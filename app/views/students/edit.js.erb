$('#estudiante').html('<%= j  (render "edit")%>');
$('#myModal').modal({
              modal: true,
              buttons: {
                Ok: function() {
                  $( this ).modal( "close" );
                }
              }
            });
$('#estudiante').slideDown(350);