<%= form_with(model: professor_participant, local: true) do |form| %>
  <% if professor_participant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(professor_participant.errors.count, "error") %> prohibited this professor_participant from being saved:</h2>

      <ul>
      <% professor_participant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :extension_id %>
    <%= form.text_field :extension_id, id: :professor_participant_extension_id %>
  </div>

  <div class="field">
    <%= form.label :professor_id %>
    <%= form.text_field :professor_id, id: :professor_participant_professor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
